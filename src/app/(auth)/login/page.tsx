export default function Login() {
    return (
      <div>
        <h1>Login</h1>
        <p>This is an example of rout group in next.js. using (path) to group components functional or using the same layout</p>
        <p> url: /login, does not need the (path)/login</p>
      </div>
    );
  }